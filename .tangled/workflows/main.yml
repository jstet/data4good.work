when:
  - event: ["push", "manual"]
    branch: ["main"]
engine: "nixery"
clone:
  skip: true
dependencies:
  nixpkgs:
    - curl
steps:
  - name: "Trigger build"
    command: "curl 'https://coolify.lan/api/v1/deploy?uuid=smgch5844ep5v507r0zujmr0&force=false' -H \"Authorization: Bearer $DEPLOY_KEY\""
